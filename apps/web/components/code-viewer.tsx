export function CodeViewer() {
  return (
    <section className="panel compact">
      <h2>Free-First Mode</h2>
      <ul>
        <li>No paid API key required by default</li>
        <li>Local static rules for bug scan</li>
        <li>Deep analysis requires confirmation</li>
        <li>Patch generation is approval-gated</li>
        <li>Cloud LLMs are optional upgrades only</li>
      </ul>
    </section>
  );
}
